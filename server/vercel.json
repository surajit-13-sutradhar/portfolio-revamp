{
    "installCommand": "npm install --legacy-peer-deps",
    "version": 1,
    "builds": [
        {
        "src": "server.js",
        "use": "@vercel/node"
        }
    ],
    "routes": [
        { "src": "/api(.*)", "dest": "/server.js" }
    ]
}